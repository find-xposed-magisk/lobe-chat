/**
 * DOM id of the root comment composer. The selection toolbar lives in the body
 * and the composer lives in the list below it — two sibling subtrees with no
 * shared ref — so the jump between them goes through the document.
 */
export const DOCUMENT_COMMENT_COMPOSER_ID = 'document-comment-composer';
